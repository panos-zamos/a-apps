# Environment variables for deployment
# Copy this file to .env and update with your values

# JWT Secret for authentication tokens
JWT_SECRET=change-this-to-a-secure-random-string

# Container registry (Digital Ocean) and tag
# Example: REGISTRY=registry.digitalocean.com/your-registry
REGISTRY=registry.digitalocean.com/holisticagency
IMAGE_TAG=latest

# Digital Ocean configuration (for deployment script)
DEPLOY_USER=root
DEPLOY_HOST=your-server-ip-or-domain
DEPLOY_PATH=/opt/a-apps

# Optional: Database backup location
# BACKUP_PATH=/path/to/backups
